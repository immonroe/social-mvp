# Supabase Configuration
# Copy this file to .env.local and fill in your actual values
# When deploying to V0, these will be automatically configured via their Supabase integration

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Service role key for admin operations (server-side only)
# Only needed for advanced operations, not required for basic MVP
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
